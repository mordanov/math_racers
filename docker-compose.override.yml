services:
  backend:
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app:cached
    environment:
      ENVIRONMENT: development
      LOG_LEVEL: DEBUG

  postgres:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"
